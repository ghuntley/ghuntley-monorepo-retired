Simple Haskell example with nix.

Demonstrates:
 - use of callCabal2nix to build from a cabal file.
 - pinning of nix to a specific hash (recommended).
 - adding tools (addBuildTools) into a nix-shell for a development environment.
